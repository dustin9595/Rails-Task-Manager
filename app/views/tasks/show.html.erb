<h1><%= @task.title %></h1>

<p><%= @task.details %></p>

<%= link_to "Edit this task", task_edit_path %>

<p>
  <% if @task.completed %>
  <%= "#{@task.completed} - This task is completed" %>
  <% else %>
  <%= "#{@task.completed} - This task is not completed yet" %>
  <% end %>
</p>

<%= link_to "Back to list", tasks_path %>
